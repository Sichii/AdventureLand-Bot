export enum Direction {
    Down = 0,
    Left = 1,
    Right = 2,
    Up = 3
}

export enum DeferredState {
    Pending = 0,
    Fulfilled = 1,
    Rejected = 2
}

export enum DeferredResult {
    Unresolved = 0,
    Resolved = 1
}