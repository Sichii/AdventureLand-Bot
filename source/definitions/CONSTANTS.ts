import { ItemType, List } from "../internal";

export class CONSTANTS {
    static readonly EPSILON = 1;
    static readonly PLAYER_WIDTH = 50;
    static readonly ENTITY_WIDTH = 25;
    static readonly HOUR_MS = 1000 * 60 * 60;
}